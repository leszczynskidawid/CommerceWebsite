import styled from "styled-components";

const InputWrapper = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: space-between;
`;
const SInput = styled.input`
  border-radius: 5px;
  font-size: 20px;
`;

export { InputWrapper, SInput };
